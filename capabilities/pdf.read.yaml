id: pdf.read
version: 1.0.0
description: Read a PDF document and extract textual content.

inputs:
  path:
    type: string
    required: true

outputs:
  text:
    type: string

  metadata:
    type: object