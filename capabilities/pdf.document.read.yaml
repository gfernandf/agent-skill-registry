id: pdf.document.read
version: 1.0.0
description: Read a PDF document and extract textual content.

requires:
  - fs.file.read
  
inputs:
  path:
    type: string
    required: true

outputs:
  text:
    type: string

  metadata:
    type: object